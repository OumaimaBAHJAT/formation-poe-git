class Validator {

    isRequired(str) {
    
    return value.length > 0 || value !== undefined;
    
    }
    
    }
    class Validator {

        isRequired(str) {
        
        return str.length > 0 || str !== undefined;
        
        }
        
        }